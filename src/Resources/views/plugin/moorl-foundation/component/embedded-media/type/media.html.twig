<video style="min-height: {{ minHeight }}"
       class="gallery-slider-item w-100 h-100 is-{{ displayMode }}"
       preload="metadata" {{ videoAttributes }}>
    <source src="{{ image.url }}" type="{{ image.mimeType }}">
    Your browser does not support the video tag
</video>
